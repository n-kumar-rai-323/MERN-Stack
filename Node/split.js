const name = "bil MURRAY";
const newname = name.slice(0, 1).toUpperCase();
const newname2 = name.slice(1, name.length).toLowerCase();

const full =
  name.slice(0, 1).toUpperCase() + name.slice(1, name.length).toLowerCase();

console.log(newname);
console.log(newname2);
console.log(full);
